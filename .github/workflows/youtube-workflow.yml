name: Latest YouTube Videos Workflow
on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_dispatch:

jobs:
  update-readme-with-youtube:
    name: Update this repo's README with latest videos
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Pull in YouTube videos
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          comment_tag_name: "YOUTUBE"
          feed_list: "https://www.youtube.com/feeds/videos.xml?channel_id=UCRa-MY1B5RiaUF0hhGrDKFg"
          # ⚠️ Replace UC_YOUR_CHANNEL_ID_HERE with your actual YouTube Channel ID
          # Find it at: https://www.youtube.com/account_advanced
          # Or from your channel URL: youtube.com/channel/UCxxxxxxx
